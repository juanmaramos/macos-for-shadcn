# Electron

Electron consumes the same `registry/styles/macos.css`. Transparent or vibrant window configuration is optional host setup and must be verified against the Electron version used by the smoke example. Chromium/Skia text can differ from AppKit/Core Text.
