# Tauri

Tauri consumes the same `registry/styles/macos.css`. Transparent or vibrant window configuration is optional host setup and must be verified against the Tauri version used by the smoke example. WKWebView rendering is generally closer to Safari than Chromium but is not AppKit.
